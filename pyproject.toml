[project]
name = "cb25d"
version = "0.1.0"
requires-python = "==3.13.9"
dependencies = ["ipykernel>=7.1.0", "matplotlib>=3.10.7"]

[build-system]
requires = ["uv_build>=0.9.9,<0.10.0"]
build-backend = "uv_build"

[tool.pyright]
typeCheckingMode = "basic"
